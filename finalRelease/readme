1. Client contains all code + resource files for the game. Put everything into a project and run. Add the resource folder to the project folder.

2. Server contains file for the server, load it into another project and run.
